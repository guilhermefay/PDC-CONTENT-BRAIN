When coding, always manage tasks using the Task Master MCP tools (e.g., `mcp_task-master-ai_get_tasks`, `mcp_task-master-ai_next_task`, `mcp_task-master-ai_set_task_status`). Follow the workflow defined in [dev_workflow.mdc](mdc:.cursor/rules/dev_workflow.mdc) and NEVER skip the approval loop or requirements described in each task/subtask.
 
• Use sempre MCP; para qualquer integração nova registre Resource/Tool/Prompt via MCP.